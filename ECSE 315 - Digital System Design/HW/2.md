# HW 2

## 1

Construct the pass network connection diagram of a freeform tree network realization of the Boolean function given below. The tree network will connect to the input of an inverter. The inverter input will be $f$. The inverter output will $f$. Use as few devices in the tree network as possible.

$f(a,b,c,d)=\sum\limits(0,5,7,9,11,12)+d(1,2,4,8,13,14)$

> [!answer]
> ![[2/tables#^1|tables]]
> 
> $f=c'+d\cdot(a\odot b)'$
> 
> ![[2/tables#^1g|tables]]

## 2

Given:

$z=\overline{g1((ABC+ABD+ACD+BCD))+g2}$
$\overline{g1}=ABCD$
$\overline{g2}=A+B+C+D$

Obtain a switch diagram realization of the negative gate expression in

### a

Complex Static CMOS gate realization

> [!answer]
> 
> ![[2/tables#^2a|tables]]

### b

Pass transistor realization using $A,B,C$ as control signals and '$0$', '$1$', $D$ as data signals.

> [!answer]
> 
> ![[2/tables#^2b|tables]]

### c

Dynamic CMOS realization

> [!answer]
> ![[2/tables#^2c|tables]]

## 3

The simple NMOS pass network into a CMOS inverter given below might seem to pass for a suitable CMOS realization of the function $AC+\overline{AB}$. Does this circuit satisfy the CMOS assertion that no power is consumed in steady state? Why or why not? Will the output voltage be degraded from nominal values?

> [!answer]
> There is no leakage during steady state as there is no path from VDD to ground when fully switched.
> 
> There may be voltage degradation as the NMOS circuit before the CMOS inverter may not have enough voltage to close the PMOS VDD gate at the end, however, due to NMOS voltage degradation.

## 4

Consider the function $G= ABCD+ABC'D'+A'B'CD+A'B'C'D'$ which functions as a comparator: when $A=B$ and $C=D$ it produces a logic $1$. This can also be written as $(AB+A'B')(CD+C'D')$. Draw the transistor-level schematic for $G$ using:

### a

N-Domino

> [!answer]
> $G'=((A+B)(A'+B'))+((C+D)(C'+D'))$
> 
> ![[2/tables#^4|tables]]

### b

NP Domino

> [!answer]
> $G'=(AB+A'B')(CD+C'D')$
> 
> ![[2/tables#^4b|tables]]

## 5

Implement $F=\overline{(a+c+b)d\overline{ab}}$ using CMOS logic with the minimum number of transistors.

> [!answer]
> $F=\overline{(a+c+b)d\overline{ab}}$
> $F=ab+d'+a'b'c'$
> $F'=(a+b+c)d(a'+b')$
> 
> 
> ![[2/tables#^5|tables]]

## 6

### a

Convert this design into one that lends itself to a domino CMOS implementation. Do not write out logic equations, minimize gates or merge signals to create complex gates without rewiring, but merely by changing the existing gates and adding and removing inverters, you are to create 5 domino gates that compute the same value for $Z$ as the circuit above. Use Demorgan's Law to transform existing gates where necessary. The inputs ($A$, $B$, $D$ and $E$) are driven by domino gates and the output, $Z$, must be able to drive a domino gate.

> [!answer]
> ![[2/tables#^6a|tables]]

### b

Draw a transistor-level schematic for your entire domino circuit.

> [!answer]
> 
> ![[2/tables#^6b|tables]]

### c

Using the part 'A)' approach, draw a transistor-level NP domino circuit for the gate-level circuit shown. Assume that the stages that generated the inputs ($A$, $B$, $D$, and $E$) are pre-charged low.


> [!answer]
> 
> ![[2/tables#^6c|tables]]

## 7

Minimize the following Boolean equations to eliminate redundancy

### a

$ab+bcd+c\overline{a}$

> [!answer]
> $b(a+cd)+c\overline{a}$

### b

$(x+y)(x+z)$

> [!answer]
> $x+yz$

### c

$a(b+c+d)+b(c+d+a)+c(d+a+b)+d(a+b+c)$

> [!answer]
> $a(b+c+d)+b(c+d)+cd$

## 8

Label the inputs of the static CMOS circuit shown so that is implements

$Z=\overline{ac+a\overline{c}bd+f+aef}$

> [!answer]
> $Z=\overline{ac+a\overline{c}bd+f+aef}$
> $Z=\overline{ac+abd+f}$
> $Z=(a'+c')(a'+b'+d')f'$
> $Z=f'(a'+c'(b'+d'))$
> 
> ![[2/tables#^8|tables]]

## 9

Draw a complete transistor level diagram to implement the function below. Use static CMOS logic. Assume that the signals and their complements are available. Use compound gates where possible. You do not need to minimize the logic. Please connect the output wires from one gate to the inputs of the next gate, all in the same diagram. Exclusive-OR is shown as $\otimes$.

$Z1=\overline{ab+c+de}$
$Z2=\overline{f\otimes g}$
$Out=\overline{Z1Z2}$

> [!answer]
> $Z=\overline{\overline{ab+c+de}~\overline{f\otimes g}}$
> $Z=(ab+c+de)+(f\otimes g)$
> $Z'=((a'+b')c'(d'+e'))(f'\otimes g)$
> 
> ![[2/tables#^9|tables]]
